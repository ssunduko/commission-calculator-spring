# CLI profile — Togglz config (same as verticalslice profile)
togglz.enabled=true
togglz.feature-enums=com.chapman.edu.commissions.architecture.verticalslice.infrastructure.config.Features
togglz.console.enabled=false

# Suppress feature flag metrics logging in CLI mode
app.feature-flags.log-summary=false
app.feature-flags.log-changes=false
